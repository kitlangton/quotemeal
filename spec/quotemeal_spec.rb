require 'spec_helper'

describe Quotemeal do
  it 'has a version number' do
    expect(Quotemeal::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
